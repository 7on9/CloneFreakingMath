<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vnbamboo.clonefreakingmath">

    <application
        android:allowBackup="true"
        android:icon="@drawable/play"
        android:label="@string/app_name"
        android:roundIcon="@drawable/play"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayActivity" android:noHistory="true">
        </activity>
        <activity android:name=".EndActivity" android:noHistory="true">

        </activity>
    </application>

</manifest>